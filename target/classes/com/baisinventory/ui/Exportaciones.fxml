<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>

<AnchorPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.baisinventory.controller.ExportacionesController"
            prefWidth="1660" prefHeight="920"
            stylesheets="@exportaciones.css">

    <!-- Contenedor central que centra todo -->
    <VBox fx:id="rootVBox" alignment="CENTER" spacing="20"
          AnchorPane.topAnchor="0" AnchorPane.bottomAnchor="0"
          AnchorPane.leftAnchor="0" AnchorPane.rightAnchor="0">

        <!-- Logo centrado -->
        <HBox alignment="CENTER">
            <ImageView fx:id="logoGeneral" fitWidth="80" fitHeight="80" preserveRatio="true">
                <image>
                    <Image url="@../images/Logo.png"/>
                </image>
            </ImageView>
        </HBox>

        <!-- Título -->
        <Label text="📦 Gestión de Exportaciones"
               style="-fx-font-size: 24px; -fx-font-weight: bold;"/>

        <!-- Formulario de exportación -->
        <VBox spacing="10"
              alignment="CENTER"
              maxWidth="400"
              style="-fx-padding: 15; -fx-background-color: #f9f9f9; -fx-background-radius: 10;">

            <Label text="➕ Nueva Exportación" style="-fx-font-size: 16px; -fx-font-weight: bold;"/>

            <HBox spacing="10" alignment="CENTER_LEFT">
                <Label text="Ubicación:" minWidth="100"/>
                <TextField fx:id="txtUbicacion" promptText="Ubicación de origen" prefWidth="200"/>
            </HBox>

            <HBox spacing="10" alignment="CENTER_LEFT">
                <Label text="Destino:" minWidth="100"/>
                <TextField fx:id="txtDestino" promptText="Destino" prefWidth="200"/>
            </HBox>

            <Button fx:id="btnCrear" text="➕ Agregar Exportación" styleClass="primary-button"/>
        </VBox>

        <!-- Tabla de exportaciones -->
        <TableView fx:id="tablaExportaciones" prefHeight="300" maxWidth="800">
            <columns>
                <TableColumn fx:id="colId" text="ID" prefWidth="60"/>
                <TableColumn fx:id="colUbicacion" text="Ubicación" prefWidth="200"/>
                <TableColumn fx:id="colDestino" text="Destino" prefWidth="200"/>
                <TableColumn fx:id="colResponsable" text="ID Responsable" prefWidth="120"/>
            </columns>
        </TableView>


        <!-- Botón volver -->
        <HBox alignment="CENTER" spacing="10">
            <Button fx:id="btnEliminar" text="🗑 Eliminar Seleccionada" styleClass="danger-button"/>
            <Button fx:id="btnVolver" text="⬅ Volver al Menú" styleClass="button"/>
        </HBox>

    </VBox>
</AnchorPane>